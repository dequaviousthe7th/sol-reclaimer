# RPC endpoint (defaults to mainnet if not set)
# For testing, use devnet: https://api.devnet.solana.com
NEXT_PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com

# Optional: Use a premium RPC for better performance
# NEXT_PUBLIC_RPC_URL=https://your-helius-rpc-url.com

# Worker URL for RPC proxy and stats API
# When set, the app uses WORKER_URL/api/rpc as the RPC endpoint (hides API keys)
# and enables global stats tracking
# NEXT_PUBLIC_WORKER_URL=https://api.solreclaimer.net
